.DS_Store
.claude

# Скомпилированный бинарник (go build)
/ether-server

# Конфиги окружений с секретами (образец — config.example.yaml)
/config*.yaml
!/config.example.yaml

# SQLite-база (пользователи, сессии) и её WAL-файлы
/*.db
/*.db-wal
/*.db-shm
